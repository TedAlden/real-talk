 const AppCard = ({children}) => {
    return (
        <div className="rounded-lg bg-white shadow  md:mt-0 xl:p-0 dark:border dark:border-gray-700 dark:bg-gray-800">
        {children}
        </div>
    )
    }

export default AppCard;